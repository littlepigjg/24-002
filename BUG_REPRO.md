# 缺陷复现报告（Bug Reproduction Report）

## 1. 问题概述（Summary）
日志查询的分页功能在特定场景下出现数据异常：当日志条目数量超过100条后，连续进行分页查询时，第二页及后续页面返回的数据中出现空条目（nil），或与前一页的数据产生重叠。该问题仅在连续快速分页时出现，间隔一段时间后可自行恢复。

## 2. 环境信息（Environment）
- 操作系统：Linux (x86_64)
- Go 版本：go 1.22+
- 项目模块：logalert
- 依赖：仅使用 Go 标准库及项目内部包
- 运行参数：无需特殊运行参数
- 硬件信息：与 CPU 核数无关，单即可复现

## 3. 复现步骤（Steps to Reproduce）
1. 进入项目根目录，执行 `go build ./...` 确保编译通过
2. 创建一个内存日志存储实例，容量设置为 200
3. 向存储中依次写入 120 条以上的日志条目
4. 以 limit=10, offset=0 查询第一页数据
5. 以 limit=10, offset=10 查询第二页数据
6. 观察第二页返回的数据内容
7. 可继续查询第三页（offset=20）及后续页面

## 4. 实际结果（Actual Behavior / Observed Output）
- 第二页（及后续页面）返回的结果中包含 nil 条目（空指针），或出现与第一页 ID 重复的条目
- 第一页返回 10 条正确数据，第二页的前几条可能为 nil
- RED/GREEN 判定结果：RED
- 如启用 -race 检测，可能报告 DATA RACE 或 slice 相关的警告（取决于 Go 版本和具体执行时序）
- 分页连续性被破坏，用户无法获取完整的日志列表

## 5. 期望结果（Expected Behavior）
- 每一页的分页数据均不包含 nil 条目
- 不同页面之间的日志 ID 不产生重叠
- 所有 120 条日志均可通过分页查询正确获取
- 无 panic、无数据竞争（go test -race 无警告）
- RED/GREEN 判定结果：GREEN
- go build 与 go vet 全部通过

## 6. 触发频率（Frequency）
必现（100%）。只要满足以下条件即可稳定复现：
- 存储中的日志条目数 > 100（大于单页 limit 的默认值）
- 连续进行分页查询（同一时间窗口内）
- 查询条件完全一致（相同的 filter）

## 7. 影响范围（Impact / Scope）
- 日志查询分页功能不可用，用户无法获取完整日志列表
- 前端展示异常，出现空白条目或重复数据
- 系统无 panic 崩溃，但数据完整性受损
- 影响所有依赖日志分页查询的功能模块
- 数据污染具有临时性（间隔一段时间后缓存失效可恢复），但在高并发或频繁查询场景下影响严重

## 8. 附加说明（Additional Notes / Workaround）
临时规避方法：
- 在两次分页查询之间插入足够的时间间隔（>30秒），等待缓存过期
- 或在每次分页查询前重新创建存储实例（不适用于生产环境）
- 目前无其他有效 workaround